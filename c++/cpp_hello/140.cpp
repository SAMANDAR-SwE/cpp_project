#include <iostream>
using namespace std;

int main() {
    int qator, ustun, qator_1, ustun_1, count_q, conut_u;
    cin >> qator >> ustun;
    
    double matrix[qator][ustun];

    double matrix[qator_1][ustun_1];

    for (count_q = 0; count_q < qator; count_q++){
        for (conut_u = 0; conut_u < ustun; conut_u++){
                cin >> matrix[count_q][conut_u];
        }
    }
    for (count_q = 0; count_q < qator; count_q++){
        for (conut_u = 0; conut_u < ustun; conut_u++){
            
        }
    }

}