#include <iostream>
#include <string.h>
#include <string>
using namespace std;

int main(){
    char text[500];
    cin.getline(text, 500);

    
    int len = strlen(text);
    int summa = 0;
    for (int count = 0; count <= len; count ++){
    }

}
